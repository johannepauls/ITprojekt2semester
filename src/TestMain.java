
import java.util.*;

public class TestMain {

    private static double temp = 0.0;
 
    private static double puls = 0.0;
    private static DataStorage database = new DataStorage();

    public static void main(String[] args) {
       /* for(int i=0;i<40;i++){
            temp = i;
            database.gemData(temp, "Temperatur");
            puls = i;
            database.gemData(puls, "Puls");
        }
        
        database.hentData("Temperatur");
        System.out.println((database.hentData("Temperatur")).toString());
        database.hentData("Puls");
        System.out.println((database.hentData("Puls")).toString());
                
   */
    }
}
